dat <- read.table("f.txt", dec = ".")
d<-dat[,1]
d
setwd("D:/vkiad/lab2")
dat <- read.table("f.txt", dec = ".")
d<-dat[,1]
d
dat <- read.table("input.txt", dec = ".")
d<-dat[,1]
d
dat <- read.table("input.txt", dec = ",")
d<-dat[,1]
d
mean(d)
disp <- var(d)
disp
sd(d)
sqrt(disp)
which.max(table(d))
median(d)
install.packages("timeDate")):
install.packages("timeDate"):
library(timeDate)
library(timeDate)
skewness(d)
kurtosis(d)
dat <- read.table("f.txt", dec = ",")
dat <- read.table("input.txt", dec = ",")
d<-dat[,1]
d
mean(d)
disp<-var(d)
disp
sd(d)
sqrt(disp)
which.max(table(d))
median(d)
install.packages("timeDate")
library(timeDate)
skewness(d)
kurtosis(d)
usech_sr <- mean(d, trim = 0.125)
usech_sr
koeff_var <- sqrt(disp) / sr_zn * 100
koeff_var <- sqrt(disp) / mean(d) * 100
sr_lin_otklon <- sum(abs(d - mean(d))) / n
sr_lin_otklon <- sum(abs(d - mean(d))) / length(d)
sr_lin_otklon
otnos_lin_otklon <- sr_lin_otklon / mean(d) * 100
otnos_lin_otklon
setwd("D:/vkiad/lab2")
setwd("C:/Users/Jan/Downloads")
setwd("C:/Users/Jan/Downloads")
setwd("C:/Users/Jan/Downloads")
setwd("D:/vkiad/lab2")
---
title: "Лабораторная работа №2. Введение в язык обработки данных R. Анализ одномерных данных"
mainfont: Arial
monofont: Consolas
output:
pdf_document:
latex_engine: lualatex
# Перед верстанием отчёта настоятельно рекомендуется изменить параметры. Более подробная информация по ним представлена в комментариях ниже.
params:
surname: "DUBOVSKIJ"
name: "JAN"
variant: "6"
filename: "input.txt"
---
cat(params$surname, params$name)
cat(params$variant)
dat <- read.table(file=params$filename, dec=",")
d <- dat[,1]
d
sort(d)
mean(d)
disp<-var(d)
disp
sd(d)
which.max(table(d))
median(d)
skewness(d)
library(timeDate)
skewness(d)
library(timeDate)
kurtosis(d)
truncated_mean <- mean(d, trim = 0.125)
truncated_mean <- mean(d, trim = 0.125)
truncated_mean <- mean(d, trim = 0.125)
the_coefficient_of_variation <- sqrt(disp) / mean(d) * 100
the_coefficient_of_variation <- sqrt(disp) / mean(d) * 100
cat(params$surname, params$name)
cat(params$variant)
dat <- read.table(file=params$filename, dec=",")
d <- dat[,1]
d
sort(d)
mean(d)
disp<-var(d)
disp
sd(d)
which.max(table(d))
median(d)
library(timeDate)
skewness(d)
library(timeDate)
kurtosis(d)
truncated_mean <- mean(d, trim = 0.125)
mean_linear_deviation <- sum(abs(d - mean)) / length(d)
mean_linear_deviation <- sum(abs(d - mean(d))) / length(d)
relative_linear_deviation <- mean_linear_deviation / mean(d) * 100
truncated_mean <- mean(d, trim = 0.125)
truncated_mean
the_coefficient_of_variation <- sqrt(disp) / mean(d) * 100
the_coefficient_of_variation
mean_linear_deviation <- sum(abs(d - mean(d))) / length(d)
mean_linear_deviation
relative_linear_deviation <- mean_linear_deviation / mean(d) * 100
relative_linear_deviation
